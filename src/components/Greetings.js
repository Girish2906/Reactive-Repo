import React from "react" ; 
const  Greetings = ({x : name , y : sirname}) => {
    return (
        <div>
            <h1>This is a normal page</h1>
            <h2>I want to become an IPS officer like {x} {y} </h2>
        </div>
    ) ; 
}

export default Greetings ; 